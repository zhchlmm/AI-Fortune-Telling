## Context

现有 JWT 登录仅用于演示，账号密码写死在代码中。为支持后续运维和安全演进，需要改为数据库账号并使用哈希。

## Goals / Non-Goals

**Goals:**
- 新增管理员用户表并接入登录流程。
- 使用哈希校验密码，避免明文对比。

**Non-Goals:**
- 不实现完整 RBAC 权限体系。
- 不实现找回密码/重置密码流程。

## Decisions

- 使用 `SHA256(salt + password)` 作为当前阶段最小哈希方案。
- 通过种子数据创建默认管理员 `admin`，保证开发环境可直接联调。
