## 1. 持久化基础设施

- [x] 1.1 引入 EF Core 与 MySQL 依赖并配置连接串
- [x] 1.2 新增 DbContext、实体与模型映射
- [x] 1.3 增加数据库初始化与基础种子数据

## 2. 控制器持久化改造

- [x] 2.1 会话接口改为数据库读写并保持分页能力
- [x] 2.2 模板、内容、订单接口改为数据库读写

## 3. 验证与文档

- [x] 3.1 完成后端构建验证
- [x] 3.2 更新任务状态并记录已知问题

## 已知问题

- 已补充 EF Core Migration（`InitialCreate`），可通过 `dotnet-ef` 管理后续变更。
- 当前执行 `database update` 时出现 MySQL 连接失败，需校验本机 MySQL 监听地址、端口和账号密码。
- 连接串与 JWT 密钥仍在配置文件中明文，后续应迁移至安全配置系统。
